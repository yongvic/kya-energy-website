type Type = {
  navigation: {
    home: string,
    about: string,
    services: string,
    contact: string,
  },
  hero: {
    title: string,
    subtitle: string,
    cta: string,
  }
};

export default Type;
